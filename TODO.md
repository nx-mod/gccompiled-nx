# TODO - gccompiled-nx

- [ ] `tools/gccompiled build`: one command from a dump to a program, as the Wii
      side has
- [ ] Read the dump directly, rather than asking for an extracted folder
- [ ] A first game translated end to end
- [ ] Per-game projects for the four candidates, so a new one is a copy rather
      than a decision
- [ ] Say in the build log which functions were answered natively and which were
      translated, so a slow frame can be read against it
